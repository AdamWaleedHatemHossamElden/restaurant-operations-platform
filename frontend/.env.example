# Public browser configuration; never place credentials or secrets in VITE_ variables.
# Local development:
VITE_API_BASE_URL=http://localhost:8080/api/v1
# Production example (set in the build environment):
# VITE_API_BASE_URL=https://api.example.com/api/v1
